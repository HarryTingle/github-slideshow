# Accounts

One file per target practice or design partner, using `../_template-account.md`.

Naming: `practice-name.md`, lowercase, hyphenated.

Empty until external discovery begins. Customer zero lives one level up in `customer-zero.md`, because it is not a prospect — it is us.
